# Workspace

## Overview

pnpm workspace monorepo using TypeScript. Each package manages its own dependencies.

## Stack

- **Monorepo tool**: pnpm workspaces
- **Node.js version**: 24
- **Package manager**: pnpm
- **TypeScript version**: 5.9
- **API framework**: Express 5
- **Database**: PostgreSQL + Drizzle ORM
- **Validation**: Zod (`zod/v4`), `drizzle-zod`
- **API codegen**: Orval (from OpenAPI spec)
- **Build**: esbuild (CJS bundle)

## Key Commands

- `pnpm run typecheck` — full typecheck across all packages
- `pnpm run build` — typecheck + build all packages
- `pnpm --filter @workspace/api-spec run codegen` — regenerate API hooks and Zod schemas from OpenAPI spec
- `pnpm --filter @workspace/db run push` — push DB schema changes (dev only)
- `pnpm --filter @workspace/api-server run dev` — run API server locally

See the `pnpm-workspace` skill for workspace structure, TypeScript setup, and package details.

## Keyboard Website (keyboard.kraza.qzz.io)

Located in `keyboard-website/`. A standalone Node.js + Express website for FlexBoard Pro.

- **Server**: `keyboard-website/server.js` (port 5000)
- **Home page**: `keyboard-website/public/index.html` — features, download button, stats
- **Admin panel**: `keyboard-website/public/admin.html` — access via `/admin` (no link on home page)
- **Admin key**: `27353135264` (hard-coded in server.js and admin.html)
- **Approvals API**: `GET /approvals.json` — serves the keyboard approval list (compatible with Android app)
- **User data**: `keyboard-website/data/users.json` — approved/blocked users
- **Settings**: `keyboard-website/data/settings.json` — download link, app version, WhatsApp number
- **Run**: `node keyboard-website/server.js`

### Admin Panel Features
- Dashboard: stats (approved, blocked, expired, lifetime) + users expiring in 7 days
- Approved Users: add/edit/delete users, set plan (monthly/halfyear/yearly/lifetime), set plan_until date, block users
- Blocked Users: add/remove blocked users, unblock back to approved
- Settings: update download APK link, app version, WhatsApp number

### Android App Approval URL
Changed from GitHub raw URL to: `https://keyboard.kraza.qzz.io/approvals.json`
Updated in `flexboard-pro/app/build.gradle.kts` (APPROVAL_URL constant).

### Update System
- `GET /update.json` — returns `{ latest_version, download_url, force_update, release_notes }`
- `POST /api/admin/upload-apk` — upload APK file (stored at `keyboard-website/apk/flexboard-pro.apk`)
- `GET /download/flexboard-pro.apk` — serve uploaded APK for download
- Admin "Settings & APK" tab: upload APK, set version, set release notes
- Android app: `UpdateChecker.kt` fetches `/update.json` every 30 min, compares with installed version
- Android app: `UpdateDialog.kt` shows non-dismissable popup (over GatedApp) when update available
- Integrated in `GatedApp.kt` — updateInfo state + overlay dialog

## Side project: FlexBoard Pro (Android keyboard APK)

`flexboard-pro/` contains a complete native Android Kotlin/Jetpack Compose IME project (separate from the pnpm monorepo). It is built in the cloud via the GitHub Actions workflow at `.github/workflows/android-build.yml` — pushing the repo to GitHub produces downloadable debug + release APK artifacts. Includes 10 modules (IME, Auto-Type, suggestions, themes, fonts, clipboard, macros, multi-language, settings, floating pointer) plus an auto-save sentences feature backed by Room.

Recent improvements (v1.11 — instant-revoke fix, 3-day free trial + paid plans, hamburger drawer settings, WhatsApp community popup, About hero card):
- **Instant approval revoke** (`security/ApprovalGate.kt`, `security/ApprovalRefreshWorker.kt`, `ime/FlexboardIMEService.onStartInputView`): the v1.10 6-hour cache let a revoked user keep typing until the next worker tick. Cache TTL dropped to **5 min** (was 6 h), `MAX_OFFLINE_MS` to **3 d** (was 7 d), and worker scheduling switched from `KEEP→UPDATE` so existing installs pick up the new interval on upgrade. New `ApprovalGate.isCacheStale()` + `invalidateCache()` helpers are called from the IME's `onStartInputView` to kick off a soft re-fetch on every fresh field bind — combined with the worker, removing an `android_id` from `Users.json` now propagates to the device within seconds, not hours. Network requests carry `Cache-Control: no-cache` + `Pragma: no-cache` headers to defeat GitHub raw / CDN edge caches. Receiver in IME service also calls `invalidateCache()` so any in-flight render after the broadcast falls through to the lock branch.
- **3-day local free trial + paid plan gating** (`security/TrialManager.kt`, `security/AccessGate.kt`, `ui/screens/PlansScreen.kt`, `ui/GatedApp.kt`, `Users.json` schema v2): brand-new `TrialManager` records trial-start in SharedPreferences (`KEY_TRIAL_START_MS`) signed with `SHA-256(signing-cert + android_id)` (`KEY_TRIAL_SIG`) — a clear-data wipe resets, but the dev's GitHub `plan_until` is the source of truth for paid users. New `AccessGate` is the unified entry point that combines `SecurityGuard` + `ApprovalGate` + `TrialManager` into a sealed `AccessState` (`Tampered`, `Blocked`, `ApprovedLifetime`, `ApprovedWithPlan(planUntilMs)`, `PlanExpired(planUntilMs)`, `TrialActive(remaining,total)`, `TrialExpired`, `OfflineUnknown(reason)`). The IME's `onCreateInputView` and `GatedApp` both consult `AccessGate.cachedState(ctx).allowsKeyboard` instead of the legacy `ApprovalGate.isApprovedCached`. `Users.json` schema v2 (same GitHub URL, `chandtricker-2/keyboardautotyper-approvals/users.json`) adds optional `plan_until` ISO-8601 UTC per approved entry — `null/missing` = lifetime, future = approved with countdown, past = `PlanExpired` (keyboard locks, user must repurchase). Pricing per spec: **$1/month**, **6 months at $5.40 (10% off)**, **1 year at $10.20 (15% off — "BEST VALUE")**. New `PlansScreen` shows live countdown (DD/HH/MM/SS chips, 1 Hz tick), 3 selectable plan cards with strikethrough discounts + per-month equivalent, **compulsory name input** (persisted to `KEY_LAST_BUYER_NAME`), and a "Buy via WhatsApp" deep-link button that pre-fills `https://wa.me/<owner>?text=…` with plan + name + device-id. `GatedApp` routes `TrialExpired` / `PlanExpired` straight to `PlansScreen` (so the user gets the purchase path, not just a static lock); `Blocked` / `Tampered` / `OfflineUnknown` still go to the simpler `LockScreen`. `FlexboardApp.onCreate` calls `AccessGate.ensureTrialStarted(ctx)` so users who only enable the IME (never opening MainActivity) still get their 3 days counted from install.
- **MainActivity layout flipped + ModalNavigationDrawer** (`ui/MainActivity.AppRoot`): per user request, hamburger (≡) moved to the **LEFT** of the top app bar and the **"FlexBoard Pro"** brand text moved to the **RIGHT**. The cramped `DropdownMenu` was replaced with a proper Material 3 `ModalNavigationDrawer` (280 dp wide, gradient-orange header) that slides in from the left edge — full-width `NavigationDrawerItem` rows with icon + label, active row highlighted with the orange accent. New "Plans" tab added between "Clipboard" and "About" (Star icon) so users can review and renew their plan from the main UI.
- **WhatsApp community popup on every launch** (`ui/MainActivity.WhatsAppCommunityDialog`): centered Material `Dialog` with a green gradient header (WhatsApp speech-bubble icon), body explaining the community, "Join WhatsApp Community" green button (deep-links to `Obf.decode(WA_COMMUNITY_URL)` → `https://chat.whatsapp.com/HBBWmgTmpNl7OvG2MCpQtR?mode=gi_t`), "Later" outlined button, and a "Don't show this again" checkbox. State is persisted via `KEY_WA_POPUP_HIDDEN` SharedPreferences flag — once the user ticks the box and dismisses, the popup never returns.
- **About screen developer hero card** (`ui/screens/AboutScreen.DeveloperHeroCard`): the small subdued developer card was rebuilt as a big highlighted hero. Multi-stop linear-gradient backdrop (deep brown → orange-tinted), gradient border, 82 dp circular monogram avatar with two-tone radial glow + gold border + bold initials, gradient brush-painted name (28 sp gold-orange), "DEVELOPER · FOUNDER" badge with orange pill chrome, team line, and "Made with ♥ in Pakistan" footer.
- **Version bump**: `versionCode 1→2`, `versionName 1.0.0→1.11.0` in `app/build.gradle.kts`.

Earlier v1.10 improvements (default look, secured branding, panel reset, install fix, background revoke, target name, complete emoji catalog, cool symbols & stylish lines, live WPM badge):
- **Live WPM (typing-speed) badge on suggestion strip** (`ime/WpmTracker.kt` new singleton, `KeyboardView.buildSuggestionRow` adds `wpmView`, `KeyboardView.refreshWpm` + `startWpmTicker`, `FlexboardIMEService.handleKey` records keystrokes + pushes immediate refresh, `onFinishInput` resets): a tiny two-line badge ("**83**" over "**WPM**") now sits between the ≡ menu button and the suggestions scroll area, showing live typing speed. Implementation: `WpmTracker` keeps an `ArrayDeque<Long>` of keystroke timestamps in a 10-second sliding window; `currentWpm() = count * 60_000 / (5 * spanMs)` using the standard 5-character-per-word definition (matches Gboard / monkeytype). Backspace deliberately NOT counted — corrections and held-down delete would otherwise spike the gauge to 200+ WPM. After 3 s of idle the badge fades to "—" via a 1 Hz coroutine ticker started in `onAttachedToWindow` and cancelled in `onDetachedFromWindow`. Tap the badge to reset the rolling window (useful for timing a fresh paragraph). The label uses `keyText & 0x00FFFFFF | 0x99000000` for a muted appearance — works across every theme without needing a new `theme.muted` field. `onFinishInput` calls `WpmTracker.reset()` so jumping from a fast WhatsApp session to a slow Notes field doesn't leave a stale 80 WPM hanging on the badge.
- **Cool Symbols + Stylish Lines tabs in emoji panel** (`EmojiData.kt` two new categories, `EmojiData.Category` extended with `columnsPerRow` / `textSizeSp` overrides, `KeyboardView.rebuildEmoji` `loadCategory` honours those per-category overrides): added two brand-new tabs to the emoji panel — **"Symbols+"** with **617 cool single-character glyphs** (stars ★ ✦, crowns ♛ ♔, hearts ♥ ❤ ❣, music ♪ ♫ 𝄞, weather ☀ ☃, religious ☮ ☯ ✝, zodiac ♈–♓, math ∞ ∑ ∫ √ ≈, currency € £ ¥ ₹ ₿, decorative arrows ➤ ➜ ➡, geometric ◆ ◇ ◉ ▲ ◀, dingbats ✿ ❀ ✎ ☞), and **"Lines"** with **94 stylish divider patterns** (━━━ ═══ ─────, gradient blocks ▁▂▃▄▅▆▇█, fancy boxes ╔═══╗ ╰───╯, sparkle dividers ✦━━━━✦, banner styles ꧁༺━━━༻꧂, hearts/arrows rows ♥━━━♥, decorative ೋ❀❀ೋ═══❀═══ೋ❀❀ೋ). To make multi-character lines actually readable, `Category` got two optional fields — `columnsPerRow` (default 8 for emojis, set to **2** for Lines so each cell is half-width) and `textSizeSp` (default 22sp, set to **13sp** for Lines so 12-15 char dividers fit). `KeyboardView.loadCategory` reads both and falls back to defaults; emoji cell `TextView`s now have `maxLines = 1` + `TruncateAt.END` so even an oversized custom string never breaks the row layout. Tab order: existing 12 emoji categories first, then Symbols+, then Lines (so muscle-memory for the standard tabs is preserved).
- **Complete 2026 emoji catalog** (`EmojiData.kt`): old curated list had ~700 hand-picked glyphs and was missing hundreds of standard emojis the user wanted. Replaced with the full Unicode 18.0 / Emoji 18.0 RGI set (released 30 Jan 2026, fetched from `unicode.org/Public/draft/emoji/emoji-test.txt`) — **1923 base emojis across 12 categories** (Smileys, Emotion, Hands, People, Hearts, Animals, Food, Activity, Travel, Objects, Symbols, Flags). Includes all 17 brand-new 17.0/18.0 additions (🫫 distorted face, 🫪 face with bags, 🫹/🫺 push hands, 🛘 trolleybus, 🪎 hairpin, 🪍 saddle, 🫍 leafless tree, 🫌 root vegetable, 🫝 apple core, 🪊 jellyfish, 🧑‍🩰 ballet dancer, etc.). Skin-tone variants (U+1F3FB..U+1F3FF) deliberately excluded from the panel — including all 5 tones per person/hand emoji would 5× the panel size and slow scrolling; user can still type them via the system picker. Tab order preserved (Gboard-style) so the layout doesn't shift for returning users. Parser kept only `fully-qualified` entries from the Unicode test file to avoid the duplicate / mis-rendering glyphs that `minimally-qualified` and `unqualified` rows produce.
- **Background approval re-check + instant lock on revoke** (`security/ApprovalRefreshWorker.kt`, `FlexboardApp.onCreate`, `FlexboardIMEService` receiver, `GatedApp` `DisposableEffect`): the original `ApprovalGate` only re-validated against the GitHub Users.json on app launch and at the 6-hour cache boundary, so a revoked user could keep typing for hours. New `ApprovalRefreshWorker` (CoroutineWorker, `androidx.work` 2.9) runs every 15 min when the device has internet, calls `ApprovalGate.evaluate(force=true)` to bypass the cache and update the SharedPrefs decision, and on `Blocked` / `NotApproved` results sends a package-scoped broadcast `com.flexboard.action.APPROVAL_REVOKED`. The IME service registers a `RECEIVER_NOT_EXPORTED` BroadcastReceiver in `onCreate` that calls `setInputView(makeImeLockView())` the moment the broadcast lands, so the next character a revoked user tries to type goes into a lock placeholder instead of the chat field. `GatedApp` mirrors this with a `DisposableEffect`-registered receiver that bumps a `refreshTick` to re-run the gate evaluation, flipping the foregrounded settings UI to the LockScreen instantly. `OfflineUnknown` results never trigger the broadcast — a single failed poll won't lock out an approved user. Worker scheduled with `enqueueUniquePeriodicWork(KEEP)` so repeated app launches don't reset the timer; `NetworkType.CONNECTED` constraint means zero battery cost while offline. No notification / toast — the only visible signal is the keyboard switching to the lock view.
- **Target Name prefix in Auto-Typer** (`SettingsStore.KEY_AT_TARGET_NAME`, `AutoTypeEngine.start`, `AutoTypeScreen` Card above Source B): new optional "Target Name" text field on the Auto-Type screen, persisted via SharedPreferences. When non-blank, every line typed by `AutoTypeEngine` is prefixed with `<name> ` (name + single space) before injection — e.g., set "Raza" and the line "salam" becomes "Raza salam". Empty value means no prefix (current behavior). Read once at session start in `start()`, so changing the field mid-session affects only the next `▶ START` press. Useful for chat apps where the user wants to address each message to a specific person without re-typing the name in every line of their source text.

Earlier v1.10 improvements:
- **Install error fix** (`app/build.gradle.kts` `release` buildType): earlier release builds were left UNSIGNED when the GitHub Actions workflow had no `KEYSTORE_BASE64` secret configured — Android then refused to install the APK with "App not installed — package appears to be invalid" (modern Android requires APK Signature Scheme v2/v3, which an unsigned APK doesn't satisfy). Release block now ALWAYS attaches a signing config: real release keystore when available, otherwise the auto-generated debug keystore as fallback. Result: every artifact GitHub Actions uploads is side-loadable. SecurityGuard's strict pinning is automatically skipped in this dev/CI mode (`IS_OBFUSCATED=false` in `generateObfConstants` when no real keystore), so the keyboard stays usable. Also bumped versionCode 3 → 4 and versionName 1.9.0 → 1.10.0 so phones recognise this as an upgrade. **NOTE for users with previous v1.9 release installed**: if v1.9 was signed with a different cert than this build (e.g., previously had real keystore secrets and now don't, or vice versa), the new APK will refuse to overwrite — uninstall the old FlexBoard first, then install the new one.

- **First-run defaults applied** (`FlexboardApp.applyDefaultsOnFirstRun` + new `SettingsStore.KEY_DEFAULTS_APPLIED` flag): a fresh install now lands on the curated pastel-blue look the user asked for. The bundled `default_keyboard_bg.png` (in `app/src/main/res/drawable-nodpi/`) is registered as the keyboard background via an `android.resource://` URI (handled by the existing `applyKeyboardBackground` path through `ContentResolver.openInputStream`). Defaults written: bg opacity 100 %, "Show key boxes over background" ON, "Apply background image to keys" ON, border style "rounded", key opacity 71 %, key text size 18 sp, key/row height 56 dp, key spacing 1 dp, 3D shadow ON. Gated by `KEY_DEFAULTS_APPLIED = "defaults_v10_applied"` so it runs ONCE per install — later customisations are never overwritten. Bumping the suffix in a future build re-applies a new curated default for upgrading users.
- **Toolbar inline icons removed; only ≡ menu remains** (`KeyboardView.refreshSuggestions` idle branch + `toolActions()` deletion): the suggestion strip used to render four duplicate toolbar icons (💣 / 📋 / Aa / ⚙) inline whenever there were no suggestion words. Those tiles already exist inside the ≡ menu's tools grid (Themes / Font / Clipboard / Auto-Type / Language / Emoji / Keyboard / Settings), so showing them in the strip too was duplication. Idle strip now stays empty — only the left-side `≡` button is visible. Tapping ≡ still opens the full 8-tile grid.
- **Keyboard always re-opens on KEYS panel** (`KeyboardView.resetToKeysPanel` + `FlexboardIMEService.onStartInputView` / `onFinishInputView` overrides): Android caches the View returned from `onCreateInputView` and reuses it across show/hide cycles, so the `panelMode` member persisted across opens — closing on Emoji / Auto-Type / Tools meant the next open landed on the same panel (the user's "wahi se open hota jaha pr close kara" complaint). New public `resetToKeysPanel()` method swaps the panel back to KEYS via `showKeysPanel()` (no-op when already on KEYS). Called from both `onStartInputView` (every fresh field bind) AND `onFinishInputView` (belt-and-braces for launchers that skip the start callback).
- **GitHub approval URL updated + space-bar branding now XOR-encrypted** (`flexboard-pro/app/build.gradle.kts` `plaintexts` map): `APPROVAL_URL` switched from `nobita1366/keyboard-approve-users/Users.json` to `chandtricker-2/keyboardautotyper-approvals/users.json`. New `SPACE_LABEL = "I love Raza"` constant added to the same XOR-encrypted obfuscation table that already protects contact info / approval URL — bound to the keystore SHA-256 so a thief who repackages with their own cert decodes garbage. `KeyboardLayouts.bottomRow` and `KeyboardLayouts.SYMBOLS` now ship `KeyDef("", " ", …, KeyType.SPACE)` (blank label) and `KeyboardView` resolves the visible text at render time via `Obf.decode(ctx, ObfConstants.SPACE_LABEL)`. The literal "I love Raza" string is therefore not present anywhere in the compiled APK that an editor could swap.

Recent improvements (v1.5 — UX polish, perf, themes):
- **Toolbar font icon** (`KeyboardView.toolActions`): the "Aa" text label was replaced with the 🔤 emoji so the inline strip carries a real ICON for the font picker. Old "Aa" rendered in whatever keyboard typeface was active and looked confusing; the emoji glyph is unambiguous.
- **Tools menu opens IN the keyboard area** (`KeyboardView` new `PanelMode.TOOLS` + `showToolsPanel` + `rebuildTools` + `buildToolTile` + `allToolActions`; old `showToolMenu` PopupWindow deleted): the ≡ button on the suggestion strip now switches the panel to a Gboard-style 4-column tile grid (Themes / Font / Clipboard / Auto-Type / Language / Emoji / Keyboard / Settings) instead of opening a floating popup. Each tile is a vertical icon-over-label button and matches the screenshot the user shared. A "⌨ Back to keyboard" pill at the bottom returns to the keys panel.
- **Auto-Type panel rebuilt for usability** (`KeyboardView.rebuildAutoType` rewrite): bigger panel (height bumped from `keyHeight*5` to `max(keyHeight*6, 320dp)`), bigger title, dedicated status badge (RUNNING / PAUSED / STOPPED), bigger 56dp action button row. **Removed**: AutoSend toggle, "Open settings" shortcut, and the separate Stop button. **Unified** Start + Stop into ONE button that toggles label + color (green-accent "▶ START" when stopped, red "■ STOP" when running). Pause stays separate and dims to 45% alpha when not actionable so users can see at a glance it's a no-op while stopped.
- **Typing performance: shift toggle no longer rebuilds the keyboard** (`KeyboardView.toggleShift` / `setShifted` / new `updateLetterCase` + `charKeyViews` field): the previous code re-ran full `rebuild()` on every shift state change — destroying ~30 TextViews + their backgrounds + their touch listeners. With the auto-cap added in v1.5, that fired on every cursor movement, producing the "slow / laggy" typing the user reported. Now `setShifted` walks the cached `charKeyViews` list and just flips `tv.text` between lower/uppercase. Symbols mode still goes through the full rebuild because the layout itself changes.
- **Typing performance: cached per-key drawables** (`KeyboardView.rebuild` + `KeyStyle` data class): the touch listener used to call `makeKeyBackground(...)` on every ACTION_DOWN and ACTION_UP — two GradientDrawable allocations per keystroke causing minor GC stalls. Each key now pre-builds its normal + pressed-state drawables once during `rebuild()` and stores them in `View.tag` (`KeyStyle.normalBg` / `KeyStyle.pressedBg`); the touch listener and `flashKeyForChar()` just swap object references. Allocation count per keypress: was 2, now 0.
- **15 new built-in themes** (`ThemeManager.BUILT_IN`): added Candy Pastel (matches the cute pastel-blue Gboard reference the user shared — pastel surface, near-white keys, deep navy text, soft pink accent), Bubblegum Pink, Mint Cream, Lavender Dream, Sunset Orange, Rose Gold, Ocean Blue, Forest Green, Midnight Purple, Cherry Red, Coffee Mocha, Lemon Yellow, Slate Gray, Cyan Wave, Sky Morning. All themes hit WCAG AA contrast between key text and key background and use distinct pressed-key shades for visible tap feedback.

Recent improvements (v1.1):
- **Auto-Type real typing**: in Direct (IME) mode each character is committed one at a time via `commitText`, so the keyboard stays open and keys appear to be pressed in real time (no paste). A "Typing speed (ms/char)" slider in the Auto-Type tab controls speed (default 35 ms).
- **Emoji panel size**: the panel now has a fixed height (~216 dp = 40 dp tabs + 3 × 44 dp grid + 44 dp bottom). Emojis scroll inside the panel — the keyboard no longer becomes fullscreen.
- **Fonts**: 20 built-in fonts (`BuiltInFonts.kt`) using Android system families always available without bundled `.ttf` files. Custom font import still works as before. New `Aa` toolbar button on the keyboard opens a popup picker that lists built-in + custom fonts with previews; tapping a font applies it immediately and the keyboard reloads.
- **Floating pointer lock = hide**: `FloatingPointerService` is preserved. When locked, the dot becomes invisible (`View.INVISIBLE`, alpha 0) but the service stays alive, so AutoType's accessibility-driven click at the saved (x, y) keeps working. Unlock makes the dot visible again.
- **About screen**: now lists creator "This app created by Kashif Raza", an "ATF team support" line and three clickable buttons that open the WhatsApp Channel, WhatsApp Group and Instagram links.

Recent improvements (v1.2):
- **Auto-Type only when keyboard is open**: typer now commits per character only if FlexBoard's IME is the active input AND has an attached input view. No accessibility-driven typing into random fields. Pressing **Start** now also calls `moveTaskToBack(true)` so the FlexBoard app drops to the background and the user lands directly on the chat app's keyboard. Each typed character flashes the corresponding key on the keyboard so it visibly looks like the user is pressing keys. Swiping FlexBoard from Recents (`MainActivity.onDestroy` while `isFinishing`) stops the auto-typer and the foreground service.
- **Send key reliability**: pointer ON → pointer click (existing behavior). Pointer OFF → real `KEYCODE_ENTER` `KeyEvent` down + up via `currentInputConnection.sendKeyEvent`, instead of committing a literal `"\n"`. Many chat apps (WhatsApp, Messenger) only send on a real Enter event, so this fixes the "Enter doesn't send" issue.
- **Translator strip (Gboard-style)**: the IME no longer rewrites text already typed in the chat field. Instead, when the translator is enabled, typing goes into a buffer shown in a strip *above* the keyboard (source on the left, live translation on the right). When the user presses Space or Enter, only the **translation** is committed to the chat field. Backspace edits the buffer. Backspace/Enter with an empty buffer fall through to normal IME behavior so the user can still edit the field.
- **Real bundled fonts**: 16 real `.ttf` files are now packaged in `app/src/main/assets/fonts/` (Anton, BebasNeue, Caveat, DancingScript, IndieFlower, Lobster, Merriweather, Merriweather-Italic, Orbitron, Pacifico, PermanentMarker, PlayfairDisplay, PlayfairDisplay-Italic, RobotoSlab, ShadowsIntoLight, SourceCodePro). `BuiltInFonts.kt` exposes them via `asset:fonts/<name>.ttf` paths, `FontManager.loadKeyTypeface` decodes them with `Typeface.createFromAsset` and caches them. Selecting a font from the Aa picker now actually changes the on-screen typeface (previously fell back to `Typeface.DEFAULT`).

Recent improvements (v1.3 — 8 user-requested fixes):
- **Smart Enter** (`FlexboardIMEService.handleKey` / `KeyboardLayouts.bottomRow`): default Enter now auto-detects the field. Browser address bar / search box / single-line "Done" fields fire the real IME action; chat-style multi-line fields still insert `\n`. Long-press on Enter is a force-newline escape hatch (`KeyDef("⏎", longPress = "\n")`). The `KEY_ENTER_NEWLINE` setting now defaults to false (smart) — set true to force newline everywhere.
- **Selection-aware backspace + long-press menu** (`FlexboardIMEService.handleKey BACKSPACE` / `KeyboardView.showBackspaceMenu`): one tap of ⌫ now deletes the entire highlighted selection (via `commitText("", 1)` after `getSelectedText`). Long-press on ⌫ pops a Select All / Copy / Cut / Paste / Delete menu. New `KeyType` cases `SELECT_ALL`, `COPY`, `CUT`, `PASTE` dispatch to `InputConnection.performContextMenuAction`.
- **Signed release builds via GitHub Actions** (`.github/workflows/android-build.yml`, `flexboard-pro/app/build.gradle.kts`): a 10000-day RSA keystore (`flexboard-release.jks`, alias `flexboard`) was generated and copied with its base64 export to `attached_assets/`. Build script reads `KEYSTORE_BASE64` / `KEYSTORE_PASSWORD` / `KEY_ALIAS` / `KEY_PASSWORD` env vars (CI) or a root-level `keystore.properties` file (local). Workflow decodes the secret to `keystore/flexboard-release.jks`, builds debug + signed release APKs, then deletes the .jks. Setup steps documented in `attached_assets/FLEXBOARD_KEYSTORE_SETUP.md`.
- **Translator UX rewrite** (`KeyboardView.buildTranslatorBar` / `handleTranslatorKey` / `translateAndCommit`): single-row layout `[src][⇄][tgt][input box weight=1][✕]` with a status line below — dropped from 92dp to ~58dp tall. Translation only fires on Enter / `.` / `?` / `!` (not space) so whole sentences are translated, fixing "ek word translate hota hai dosra nahi". Failure path commits original text + shows actionable hint ("connect Wi-Fi to download <lang> pack" / "set source language manually").
- **Background image stability** (`KeyboardView.applyKeyboardBackground`): removed `setTileModeXY(CLAMP, CLAMP)` (which silently overrode `gravity` and made the image stretch to bounds, drifting up/down whenever a panel toggled the keyboard height). Anchor changed from `CENTER | FILL` to `BOTTOM | FILL_HORIZONTAL` so the picture stays pinned to the bottom of the keyboard regardless of which panel is open.
- **About screen** (`AboutScreen.kt`): copy now reads "This app is developed by Kashif Raza." + "Support the ATF Team by joining our social platforms." Three buttons retained — WhatsApp **Channel**, WhatsApp **Community** (renamed from Group), Instagram `@urkashif_raza`.
- **App-minimize safety** (`AutoTypeEngine.trySend` + `MainActivity.onResume`): hard guard at the top of `trySend` skips pointer / IME / accessibility clicks when `injector == null` (i.e., FlexBoard IME is not bound to a chat field). When the FlexBoard activity comes back to the foreground while a typer job is mid-flight, `onResume` calls `AutoTypeEngine.stop()` + `AutoTypeForegroundService.stop()` so no stray clicks fire on the home screen / random apps.

Recent improvements (v1.4.3 — smart auto-send, no floating pointer needed):
- **Auto-detect send action** (`AutoTypeEngine.trySend` "auto" branch rewritten; `FlexboardAccessibilityService.currentForegroundPackage` + `isKnownChatApp` + `KNOWN_CHAT_APPS` set added): the floating sender pointer is no longer required. After each typed line the engine now reads `rootInActiveWindow.packageName` and routes the send action by app type. Chat apps (WhatsApp / WA Business / Messenger / Messenger Lite / Telegram / Telegram X / Instagram / Discord / Snapchat / Viber / Google Messages / SMS) → up to 3 retries of `pressSend()` with 120 ms gaps using the per-app resource-id table (`com.whatsapp:id/send`, etc.) so the click lands on the LIVE position of the send button — no more "send button neechy ho gaya" misses when the keyboard opens / closes / panel toggles. Browsers, search boxes, single-line login forms → `sender?.invoke()` (real KEYCODE_ENTER + performEditorAction fallback) so Enter correctly fires Go / Search / Done. Chat-app branch never falls through to IME-Enter (which would insert a stray "\n" instead of sending). The floating pointer remains as an optional fallback that only fires for apps not in the supported list AND only when the user has explicitly enabled the dot.
- **AutoTypeScreen UI cleanup** (`AutoTypeScreen.kt`): the "Send method" filter chips (Auto / IME action / Scan button / Pointer click) were the wrong default for most users — half of crash reports started with a wrong send-method pick. Replaced with a "Smart auto-send" info card that explains the routing in plain language + a "Show advanced send options" toggle that hides the chips behind one tap (still available for power users). Footer help text rewritten from a 5-step floating-pointer setup to a 3-step "open chat field → press Start" flow.
- **Generic keyword scan now restricted to known chat apps** (`FlexboardAccessibilityService.pressSend`): the previous code would scan any window's accessibility tree for nodes whose content-description / text matched "send" / "submit" / "post" — that occasionally tapped a "Send Friend Request" link or an unrelated webpage button when the user had a non-chat app in the foreground. The generic scan now only runs after `isKnownChatApp(pkg)` returns true; for everything else `pressSend()` returns false so the engine falls through to IME Enter instead.

Recent improvements (v1.4.2 — 4 user-requested fixes):
- **Custom font import removed; 36 built-in fonts shipped** (`BuiltInFonts.kt` rewritten; `ThemeScreen.kt` "Custom Fonts" card replaced with "Built-in Fonts" picker; `MainActivity.kt` `pickFont` ActivityResultLauncher + `onPickFont` plumbing deleted): the old "Add Font File" flow + per-row Delete buttons are gone. The new `BuiltInFonts.ALL` catalog has 36 entries — a mix of bundled `.ttf` assets (Anton, BebasNeue, Caveat, DancingScript, IndieFlower, Lobster, Merriweather + Italic, Orbitron, Pacifico, PermanentMarker, PlayfairDisplay + Italic, RobotoSlab, ShadowsIntoLight, SourceCodePro) cross-multiplied with `Typeface.NORMAL` / `BOLD` / `ITALIC` style overlays, plus system-family pseudo-paths (`system:sans-serif-light`, `system:sans-serif-medium`, `system:sans-serif-black`, `system:sans-serif-condensed`, `system:serif`, `system:cursive`) so the picker has visibly distinct entries without bundling extra MB of fonts. `BuiltInFonts.typefaceFor` decodes the path correctly for each tier (`asset:` → `createFromAsset`, `system:` → `Typeface.create(family,…)`) and the cache key now includes the style flag so "Merriweather Bold" and "Merriweather" no longer collide in the LRU.
- **Toolbar + suggestion strip merged into ONE row** (`KeyboardView.kt` `buildSuggestionRow` / `refreshSuggestions` rewrite; `toolbarRow` + `toolbarHost` field declarations removed; `init {}` and `reload()` no longer add a second strip): the keyboard used to render two stacked top rows (suggestions + scrolling toolbar) consuming ~76dp. Now there's a single 42dp strip with `[≡ menu] [scroll area]`. The scroll area auto-switches: when `getSuggestions()` returns words → renders suggestion words; when empty → renders the toolbar icons (AT / 📋 / 🌐 / 😀 / ⌨ / Aa / ⚙) inline. The `≡` button always opens a popup listing every toolbar action (with friendly labels like "AT  ·  Auto-Type") so the user can reach Settings even mid-sentence when suggestions are filling the strip. `addToolbarButton` was promoted to a free helper that takes a parent `LinearLayout`, so the same code drives both the inline-icons branch and the popup menu.
- **"FlexBoard Pro" placeholder removed from the strip** (`KeyboardView.refreshSuggestions`): the empty-state text "FlexBoard Pro" that used to sit at half-opacity above the keys when there was nothing to suggest is gone — that real estate now hosts the inline toolbar icons (see above).
- **Space key shows "I love Raza" branding** (`KeyboardLayouts.bottomRow` + symbols layout space `KeyDef`; `KeyboardView` letter-renderer `KeyType.SPACE` branch now uses `key.label` instead of an empty string): typing the spacebar still inserts a normal space (`output = " "`) but the visible label on the long bar now reads "I love Raza" the same way the symbols-toggle key reads "abc/123".

Recent improvements (v1.4.1 — built-in theme fix + live preview):
- **Built-in themes now actually apply** (`ThemeManager.setTheme`, `KeyboardView.theme`, `FlexboardIMEService.prefsListener`): two latent bugs were stacking. (1) `setTheme` wrote `KEY_THEME` in one `apply()` then the five color overrides in a SECOND `apply()` — the listener fired on the first commit and ran `reload()` while the per-color prefs still held the previous theme's values, so the keyboard re-rendered with stale colors. Combined into a single `edit()` so all six keys commit atomically. (2) `KeyboardView.theme` was a `val` initialized at construction time, so even when `reload()` ran after a real theme change, every `theme.keyBg` / `theme.keyText` / `theme.accent` reference still pointed at the originally captured object. Changed to `var` and re-assigned via `ThemeManager.current(context)` at the top of `reload()`. Also added `KEY_KEY_BG`, `KEY_KEY_TEXT`, `KEY_KB_BG`, `KEY_SUGG_BG`, `KEY_PRESSED` to the listener's `when` so per-color edits also trigger reload.
- **Live mini-keyboard preview in ThemeScreen** (`MiniKeyboardPreview` composable + `themeTick` state): added a small non-interactive Compose mock at the top of the Theme & Appearance screen that renders the suggestion strip + two letter rows + space + Enter using the EXACT colors returned by `ThemeManager.current(ctx)`. A `themeTick` counter bumps every time the user picks a built-in theme; the preview's `remember(themeTick)` recomputes the theme and recolors instantly — no need to open a chat field to verify. The existing OutlinedTextField for opening the real IME is kept below for full-keyboard validation.

Recent improvements (v1.4 — 5 user-requested fixes):
- **Translator removed entirely** (`TranslateEngine.kt` deleted; ML Kit `translate` + `language-id` deps removed from `app/build.gradle.kts`; `KEY_TRANSLATE_*` keys removed from `SettingsStore`; all translator UI/handlers/state stripped from `KeyboardView` and `FlexboardIMEService`). User asked to drop it — keyboard now boots without ML Kit weight (~6 MB lighter APK).
- **Floating sender SEND fires reliably after AutoType line** (`FloatingPointerService.startSend` / `endSend`): `removeView`/`addView` calls were happening on the AutoTypeEngine's IO/coroutine thread → silently throwing `CalledFromWrongThread` and the system never delivered the synthetic touch. Wrapped both in `mainHandler.post {}` so the WindowManager mutation runs on the UI thread. Bumped `AutoTypeEngine.pointerClick()` settle delay 120 → 200 ms and hide duration 450 → 500 ms to give the dispatch full headroom. "Lock = invisible" preserved (alpha 0 + `View.INVISIBLE`).
- **Suggestions now context-aware Roman Urdu + bigrams** (`SuggestionEngine.kt` rewrite): added `BUILTIN_UR_LATIN` (~180 common Roman Urdu words: kya, hai, nahi, theek, accha, kaise, ho, kar, mein, etc.) and a curated bigram table (UR + EN: "kya"→"hai", "ap"→"kaise", "how"→"are", etc.) seeded into the Room `dictionary_words` table on first run. Bigrams are stored as rows where `word = next` and `nextWord = previous` (matches the existing DAO query `WHERE nextWord = :prevWord`). `suggest(prefix, prevWord)` now returns next-word predictions when prefix is empty, and bigram-weighted completions when typing. `learnSentence` walks pairs and persists every `(prev, next)` it sees, so the keyboard learns the user's actual phrasing over time. `FlexboardIMEService.updateSuggestions` already passes `prevWord` from text-before-cursor — no change needed there.
- **Custom theme background no longer changes keyboard size** (`KeyboardView.applyKeyboardBackground` → `NoIntrinsicBitmapDrawable`): replaced stock `BitmapDrawable` (which reports the bitmap's pixel width/height as intrinsic + minimum size, causing the parent LinearLayout to grow when a tall image was picked) with an inner `NoIntrinsicBitmapDrawable` that returns `-1` from `getIntrinsicWidth/Height` and `0` from `getMinimumWidth/Height`. The image still renders (anchored bottom + scaled via gravity flags from v1.3) but contributes zero size to layout — keyboard height is now driven solely by the user's "Key height" slider regardless of image dimensions.
- **AutoTyper + sender PAUSE on app close/minimize, not stop** (`MainActivity.onDestroy` / `onStop` / `onResume`; `AutoTypeScreen` callbacks): pressing Start now calls `moveTaskToBack(true)` from inside `MainActivity.onStartAutoType` (and the new `onResumeAutoType`) so AutoTypeScreen no longer initiates the move itself — this lets `MainActivity` record `lastStartAtMs` immediately before backgrounding. `onStop()` checks if the system-triggered pause happened within 2 s of `lastStartAtMs` (the Start-induced minimize) — if so, no pause; otherwise calls `AutoTypeEngine.pause()` so a manually-minimized app freezes the typer mid-line. `onDestroy` (swipe from Recents) and `onResume` (re-foreground) likewise call `pause()` instead of `stop()`. Foreground service stays alive across pause because `state.running` remains true. Renamed compose param `onResume` → `onResumeAutoType` to avoid colliding with the Activity lifecycle callback.

Recent improvements (v1.3.1 — custom font fix):
- **Custom .ttf fonts now actually apply** (`FontManager.loadCustomTypeface`, `importFont`, `setActive`): the previous `Typeface.createFromFile(path)` call silently fell back to `Typeface.DEFAULT` on many OEM ROMs (Xiaomi MIUI, Vivo / Oppo ColorOS, certain Samsung builds) for valid `.ttf` files containing DSIG signature tables, GSUB extensions, or variable-font axes — so the user picked a custom font and saw Roboto. Fixed with a 3-tier loader: (1) `Typeface.Builder(file).build()` on API 26+ which uses Android's modern Minikin loader and accepts a much broader font format set, (2) fallback to `createFromFile`, (3) explicit detection of "loader returned `Typeface.DEFAULT`" treated as failure. `importFont` now validates the file BEFORE listing it — invalid fonts are deleted and the user gets a clear toast ("Couldn't load <name>. The .ttf may be unsupported"). `setActive` now invalidates the typeface cache before writing the pref so the SharedPreferences listener can never read a stale cached typeface for the new path. The on-keyboard `Aa` picker preview shares the same loader so what you see in the picker is what lands on the keys.
